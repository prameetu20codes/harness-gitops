apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: web-dev
resources:
  - ../../base
replicas:
  - name: web
    count: 1
configMapGenerator:
  - name: web-nginx-conf
    files:
      - default.conf
